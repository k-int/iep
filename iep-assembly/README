Setup for dev env:

mysql> create database iepdev default charset utf8 collate utf8_bin;
mysql> grant all on ipedev.* to 'k-int'@'localhost';
mysql> grant all on ipedev.* to 'k-int'@'localhost.localdomain';
mysql> grant all on ipedev.* to 'k-int'@'%';
